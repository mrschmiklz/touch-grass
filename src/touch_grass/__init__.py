"""touch-grass — let an AI agent touch grass (a real keyboard) over BLE via an ESP32.

A long-running MCP server owns the ESP32's serial link and exposes physical
keyboard control as typed tools.
"""

__version__ = "0.1.0"
