"""touch-grass — let an AI agent touch grass (a real keyboard + mouse) over BLE via ESP32s.

Long-running MCP servers own the ESP32 serial links and expose physical keyboard
and mouse control as typed tools (one server instance per device).
"""

__version__ = "0.2.0"
